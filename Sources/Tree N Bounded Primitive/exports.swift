// exports.swift
// Bounded type module re-exports: the base `Tree N Primitive` (Tree.N namespace,
// Node, ChildSlot, InsertPosition shared with the base) which itself re-exports
// the `Tree` shell and backings.

@_exported public import Tree_N_Primitive
